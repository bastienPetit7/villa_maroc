


{% for rubrique,messages in app.flashes %}

    {% for message in messages %}
        <div class="col-12 my-2">
            <div class="alert alert-{{ rubrique }} divAlert mb-0">
                <p class="mb-0 mt-2">{{ message }}</p><br/>
            </div>
        </div>
    {% endfor %}

{% endfor %}